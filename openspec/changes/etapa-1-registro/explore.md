# Explore — `etapa-1-registro`

> Generated by sdd-propose (the explore + propose flow runs together for this change).
> Full content stored in Engram topic `sdd/etapa-1-registro/explore` (use `mem_get_observation`).

## Source PRD
- `prd-etapa-1-registro.md` — version **1.1** (2026-06-10), 1567 lines.
- 5 objectives, 16 explicit out-of-scope items, full DB / API / auth / frontend / config / testing / DoD.
- Two v1.1 deltas vs v1.0: split `users` → `company_users` + `platform_users`; new `roles` table with `COMPANY`/`PLATFORM` scope.

## Open Questions Resolved by Proposal Decisions
The proposal records 7 explicit decisions that close PRD ambiguities. Each is cited with PRD line numbers. See `proposal.md` → "Key Decisions" section.

## Risks Discovered
| # | Risk | Source |
|---|------|--------|
| R1 | **Stack version mismatch**: PRD targets Spring Boot 3.5.x; repo has Spring Boot 4.0.6 (from sdd-init). | pom.xml (sdd-init verified) vs PRD §Stack (line 109) |
| R2 | **Missing starters**: `pom.xml` has only `spring-boot-starter` + `spring-boot-starter-test`. No `web`, no `data-jpa`, no `security`, no `flyway`, no `validation`, no `oauth2-resource-server`, no `postgresql`. | sdd-init + PRD §Stack (lines 106-130) |
| R3 | **Backend not git-initialized** — no commits / branches / PRs possible until `git init` runs. | sdd-init + repo state |
| R4 | **No Playwright** in `frontend/` — PRD says E2E with Playwright (line 141). | sdd-init verified |
| R5 | **No coverage tooling** (JaCoCo backend, istanbul/vitest coverage frontend) — PRD demands >80% / >70% (lines 1507-1508). | sdd-init verified |
| R6 | **No linters / formatters wired** (Spotless, Checkstyle, ESLint, Prettier all missing from build scripts) — PRD §Code Conventions (lines 1267-1299). | pom.xml / package.json |
| R7 | **PRD §Stack drift**: it lists Lombok, MapStruct, Bucket4j, Sentry, SpringDoc, jjwt explicitly; none declared. v1.1 added `roles` table but PRD line 1275 still says "`users`" in naming conventions table. | PRD lines 113-130, 1275 |
| R8 | **No docker-compose** in repo root — PRD shows a sample at line 1401-1422. | repo root ls |
| R9 | **Stale package tree** at PRD lines 1190-1236 — PRD itself flags this in footnote (line 1238) and gives the corrected layout (lines 1239-1265). Follow the corrected layout. | PRD lines 1190-1265 |
| R10 | **No Testcontainers** in backend — PRD requires RLS IT (line 1461) and several other ITs (lines 1458-1463). | sdd-init + PRD §Testing |
| R11 | **3-datasource wiring** (company / system / platform) is the most architecturally risky piece — easy to leak RLS bypass to the wrong DataSource. | PRD lines 490-494 |

## Strict TDD Implications
- TDD is **ACTIVE** for both repos (openspec/config.yaml `apply.tdd: true`).
- Every **behavior** task in sdd-tasks MUST land as: failing test → make it pass → refactor. No code without a test in the same commit.
- **Chore** tasks (deps, config, git init, package layout, Flyway versioned files) are exempt from red-green but MUST keep the suite green at the end of each commit.
- The TDD posture is especially important for: validators (slug, CUIT, password), JWT issuance/validation, refresh-token rotation, RLS isolation, and the lockout counter. These are the high-stakes paths.

## Out-of-Scope Honored
The proposal explicitly **excludes** all 16 items from PRD §"Fuera de alcance" (lines 39-56) and the v1-prep items in §"Email verification (preparado, no activo en v1)" (lines 932-959) that stay non-active, and the entire §"🛡️ Platform Admin (preparado, no UI en v1)" (lines 1124-1184) UI portion.

## Prerequisite Chore
A separate `chore/bootstrap-repos` change is recommended **before** any feature work in `etapa-1-registro`:
- `git init` backend/
- Add missing Spring Boot starters: web, data-jpa, security, oauth2-resource-server (or stay with custom JWT), validation, flyway-core, flyway-database-postgresql, postgresql
- Add jjwt 0.12.x, Bucket4j 8.x, SpringDoc 2.7.x, Sentry 7.x, Micrometer, MapStruct 1.6.x, Lombok 1.18.x, Spotless 2.43.x
- Add Spring Boot 3.5 vs 4.0 decision (likely keep 4.0.6 — see proposal key decision)
- Add Playwright to frontend/, vitest coverage config, Tailwind 4 PostCSS config
- Add docker-compose.yml at repo root
- Add JaCoCo backend, coverage thresholds, CI workflows

This is a **blocking** prerequisite. The proposal calls it out so sdd-tasks can split it out cleanly. The orchestrator will surface this to the user for go/no-go.

## What Carries Forward
- Explore → Proposal: covered here.
- Proposal → Spec: see `proposal.md` "Capabilities" section. sdd-spec will read it.
- Spec → Design: design.md will capture the 3-datasource wiring and RLS aspect in detail (sequence diagrams required by openspec rules).
- Tasks: TDD-first slicing, commit-per-work-unit (400-line budget).
